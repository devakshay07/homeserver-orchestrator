# FastAPI Skill
- Use `FastAPI` with `APIRouter`.
- Use `pydantic` v2 for all models.
- Always include a `/health` endpoint returning `{"status": "ok"}`.
- Use async def for endpoints unless doing blocking CPU work.
- Use `uvicorn` to run.
