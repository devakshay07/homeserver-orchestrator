# CLI Tool Skill
- Use `argparse` or `click` for argument parsing.
- Entrypoint should be a `main()` function.
- Add `if __name__ == '__main__': main()`
- Use `sys.exit(0)` on success, `sys.exit(1)` on error.
